# A document that claims nothing

This document has no `ts` fence at all - only a console block, which
should never be reachable as a "document" the harness can run.

```console
nothing ever runs
```
